
spring:
  application:
    name: auth-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/users
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
server:
  port: 8081